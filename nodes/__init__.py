"""Node classes for the udi-Virtual-pg3 NodeServer/Plugin.

Import node classes from their modules directly, e.g.
``from nodes.VirtualSwitch import VirtualSwitch``. Avoid eager imports here so
test collection does not load every node (and udi_interface) up front.
"""
